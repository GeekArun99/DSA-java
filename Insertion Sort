public static void InsertionSort(int [] nums){
for(int i=1; i<nums.length; i++){
       int curr = i;
       int prev= i-1;
    while(i-1>=0 && nums[prev]>nums[curr]){
          nums[prev+1]= nums[prev];
           prev--;
      }
    arr[prev+1] = arr[curr];
   }
}
